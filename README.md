"# GoodTimes"

#include <stdio.h>

int main(argc, char**argv)
{
int numApples = 0;
  printf("how many apples?");
  scanf("%d", &numApples);
  printf("%d? Thats a lot of apples!", numApples);
  
return 0;
}
